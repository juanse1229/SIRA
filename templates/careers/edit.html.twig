{% extends 'base.html.twig' %}

{% block title %}Edit Careers{% endblock %}

{% block body %}
    <h1>Edit Careers</h1>

    {{ include('careers/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_careers_index') }}">back to list</a>

    {{ include('careers/_delete_form.html.twig') }}
{% endblock %}
